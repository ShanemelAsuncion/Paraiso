import React from 'react';
import { Colors, Spacing, FontFamily, FontSize} from './styles';

export default function DealsButton ({ children }){
    return (
        <button>{children} </button>
    )

}